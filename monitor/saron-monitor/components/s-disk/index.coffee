
module.exports = class HardDrive
  view: __dirname

#  This is called on the server and in the browser
#  init: (model) ->
#    @server = model.at 'server'

#  This is called only in the browser
#  create: (model) ->
#
#    model.on 'all', 'data**', (path, method, value) ->
#      drives = []
#      model.set 'drives', drives